extends base
mixin overviewBox(lable , text , icon)
    .overview-box__detail
        svg.overview-box__icon
            use(xlink:href=`/img/icons.svg#icon-${icon}`)
        span.overview-box__label= lable
        span.overview-box__text= text

mixin overview-box-user(role , name , photo )
    .overview-box__detail
        img.overview-box__img(src='/img/users/photo', alt='Lead guide')
        span.overview-box__label role
        span.overview-box__text name
block content
    section.section-header
        .header__hero
            .header__hero-overlay &nbsp;
            img.header__hero-img(src=`/img/tours/${tour.imageCover}`, alt=`${tour.name}`)

        .heading-box
            h1.heading-primary
                span= tour.name
            //.heading-box__group
                .heading-box__detail
                    svg.heading-box__icon
                        use(xlink:href='/img/icons.svg#icon-clock')
                    span.heading-box__text= `${tour.duration}day`
                .heading-box__detail
                    svg.heading-box__icon
                        use(xlink:href='/img/icons.svg#icon-map-pin')
                    span.heading-box__text= tour.startLocation.address
    section.section-description
        


        .description-box
            h2.heading-secondary.ma-bt-lg About the park camper tour
            p.description__text=tour.description
            //Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur.
            //p.description__text Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum!
        .overview-box
            div
                .overview-box__group
                    h2.heading-secondary.ma-bt-lg Quick facts
                    +overviewBox("Next date ", "August 2021", "calendar ")
                    +overviewBox("Difficulty ", tour.difficulty, "trending-up ")
                    +overviewBox("Participants ",`${tour.Participants} people`, "trending-up ")
                    +overviewBox("Rating",`${tour.ratingsAverage}`, "trending-up ")
    section.section-pictures
        .picture-box
            img.picture-box__img.picture-box__img--1(src=`/img/tours/${tour.images[0]}`, alt='The Park Camper Tour 1')
        .picture-box
            img.picture-box__img.picture-box__img--2(src=`/img/tours/${tour.images[1]}`, alt='The Park Camper Tour 1')
        .picture-box
            img.picture-box__img.picture-box__img--3(src=`/img/tours/${tour.images[2]}`, alt='The Park Camper Tour 1')

    section.section-cta
        .cta
            .cta__img.cta__img--logo
                img(src=`/img/logo-white.png`, alt='Natours logo')
            img.cta__img.cta__img--1(src=`/img/tours/${tour.images[1]}`, alt='pgoto')
            img.cta__img.cta__img--2(src=`/img/tours/${tour.images[2]}`, alt='photo')
            .cta__content
                h2.heading-secondary What are you waiting for?
                p.cta__text Buy now make yout life better 💰

                if user
                    form#checkout-form(data-tour-id=`${tour.id}`)
                        //label(for='currency') 
                        //select#currency(name='currency')
                            option(value='usd') USD
                            option(value='SAR') SAR
                            // Add more options for other currencies as needed
                        //button(type='submit') Checkout
                        //input#quantity(type='number' name='quantity' min='1' value='1') 
                        
                       
                            
                        //<label for="phone">Phone Number:</label>
                        //<input type="number" id="phone" name="phone">
                        //button.btn.btn--green.span-all-rows#book_id( type='submit') BUY NOW !
                    a.btn.btn--green.btn--small(href=`/api/v1/booking/to-cart/${tour._id}`) Cart 

                else
                   a.btn.btn--green.span-all-rows(href="/login") Login to book tour
                  




                    